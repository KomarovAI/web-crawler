# 📖 Руководство по использованию GitHub Actions Workflows

## Обзор

Этот репозиторий содержит два workflow для работы с веб-сайтами:

1. **`download-site.yml`** — загружает веб-сайт и создаёт артефакт
2. **`deploy-pages.yml`** — развёртывает артефакт в другой репозиторий

---

## 🚀 Workflow 1: Download Website Archive

### Назначение
Загружает веб-сайт с помощью `wget` и сохраняет как артефакт GitHub Actions.

### Как запустить

1. Перейдите в **Actions** → **Download Website Archive**
2. Нажмите **Run workflow**
3. Заполните параметры:

#### Параметры

| Параметр | Описание | Обязательный | По умолчанию |
|----------|----------|--------------|---------------|
| `url` | URL сайта для загрузки | Нет | `https://callmedley.com` |
| `depth_level` | Глубина crawl (1-4) | Нет | `2` |
| `output_dir` | Имя директории (только a-z, 0-9, -, _) | Нет | `site_archive` |
| `resumeUrl` | N8N webhook URL (опционально) | Нет | — |

#### Уровни глубины

- **1** — только главная страница
- **2** — главная + дочерние страницы (рекомендуется)
- **3** — до 3 уровней вложенности
- **4** — глубокий crawl (может быть медленным)

### Результат

После успешного выполнения:

1. В **Summary** workflow будет показано:
   ```
   Artifact: site_archive-20479494022
   Files: 127
   Size: 45MB
   Time: 23s
   ```

2. **Скопируйте полное имя артефакта** (например, `site_archive-20479494022`)

3. Артефакт доступен 30 дней

### Пример

```yaml
Inputs:
  url: https://example.com
  depth_level: 2
  output_dir: my_site
  
Результат:
  Artifact name: my_site-20479494022
```

---

## 📤 Workflow 2: Deploy Website to Repository

### Назначение
Загружает артефакт из предыдущего workflow и развёртывает в целевой репозиторий.

### Предварительные требования

1. ✅ Артефакт создан через `download-site.yml`
2. ✅ У вас есть Personal Access Token (PAT) с правами `repo`
3. ✅ PAT добавлен в Secrets как `EXTERNAL_REPO_PAT`

#### Создание Personal Access Token

1. GitHub → **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
2. **Generate new token (classic)**
3. Выберите scopes:
   - ✅ `repo` (Full control of private repositories)
4. Скопируйте токен
5. В репозитории: **Settings** → **Secrets and variables** → **Actions** → **New repository secret**
   - Name: `EXTERNAL_REPO_PAT`
   - Value: ваш токен

### Как запустить

1. Перейдите в **Actions** → **Deploy Website to Repository**
2. Нажмите **Run workflow**
3. Заполните параметры:

#### Параметры

| Параметр | Описание | Обязательный | Пример |
|----------|----------|--------------|--------|
| `artifact_name` | **Полное имя артефакта** из предыдущего workflow | ✅ Да | `site_archive-20479494022` |
| `source_run_id` | Run ID источника (если артефакт из другого run) | Нет | `20479494022` |
| `target_repo` | Целевой репозиторий | ✅ Да | `KomarovAI/archived-sites` |
| `target_branch` | Целевая ветка | Нет | `main` |
| `commit_message` | Сообщение коммита | Нет | `chore: deploy website` |

### Результат

После успешного выполнения:

```
═══════════════════════════════════════════════════════
📊 DEPLOYMENT SUMMARY
═══════════════════════════════════════════════════════
Source Artifact: site_archive-20479494022
Target Repository: KomarovAI/archived-sites
Target Branch: main
───────────────────────────────────────────────────────
Files Deployed: 127
Archive Size: 45MB
───────────────────────────────────────────────────────
✅ Status: DEPLOYED SUCCESSFULLY
Commit SHA: abc123def456
🔗 View: https://github.com/KomarovAI/archived-sites/commit/abc123def456
═══════════════════════════════════════════════════════
```

---

## 🔄 Полный пример использования

### Сценарий: Загрузить и развернуть сайт example.com

#### Шаг 1: Загрузка сайта

1. Actions → **Download Website Archive** → Run workflow
2. Inputs:
   ```
   url: https://example.com
   depth_level: 2
   output_dir: example_com
   ```
3. Дождитесь завершения (≈30 сек)
4. Скопируйте имя артефакта из Summary: `example_com-20479494022`

#### Шаг 2: Развёртывание

1. Actions → **Deploy Website to Repository** → Run workflow
2. Inputs:
   ```
   artifact_name: example_com-20479494022
   target_repo: KomarovAI/archived-sites
   target_branch: main
   commit_message: feat: add example.com archive
   ```
3. Дождитесь завершения (≈15 сек)
4. Проверьте целевой репозиторий

---

## ❓ Troubleshooting

### Ошибка: "Artifact not found for name: XXXXX"

**Причина:** Неправильное имя артефакта или артефакт истёк.

**Решение:**
1. Проверьте точное имя артефакта в Summary предыдущего workflow
2. Имя должно быть в формате: `{output_dir}-{run_id}`
3. Артефакты хранятся 30 дней — возможно, артефакт удалён

**Пример правильного имени:**
```
✅ site_archive-20479494022
❌ 20479494022
❌ site_archive
```

### Ошибка: "Unable to download artifact: 404 Not Found"

**Причины:**
1. Артефакт из другого workflow run
2. Не указан `github-token`

**Решение:**
Если артефакт из другого run, укажите `source_run_id`:
```yaml
artifact_name: site_archive-20479494022
source_run_id: 20479494022  # ← Добавьте это!
```

### Ошибка: "remote: Permission to XXX/YYY.git denied"

**Причина:** Отсутствует или неправильный Personal Access Token.

**Решение:**
1. Создайте новый PAT с правами `repo`
2. Добавьте в Secrets репозитория как `EXTERNAL_REPO_PAT`
3. Проверьте срок действия токена

### Workflow зависает на "Download artifact"

**Причина:** Большой размер артефакта или проблемы с сетью.

**Решение:**
1. Проверьте размер артефакта в Summary предыдущего workflow
2. Если >500MB, рассмотрите использование Git LFS
3. Попробуйте запустить workflow заново

### Ошибка: "Archive is empty or doesn't exist"

**Причина:** Предыдущий workflow не создал файлы.

**Решение:**
1. Проверьте логи `download-site.yml`
2. Убедитесь, что wget успешно загрузил файлы
3. Проверьте доступность целевого URL

---

## 🔧 Технические детали

### Артефакты

- **Формат имени:** `{output_dir}-{github.run_id}`
- **Срок хранения:** 30 дней (настраивается)
- **Максимальный размер:** 10 GB (ограничение GitHub)
- **Compression:** level 0 (без сжатия для скорости)

### Permissions

#### download-site.yml
```yaml
permissions:
  contents: read
  actions: read
```

#### deploy-pages.yml
```yaml
permissions:
  contents: read
  actions: read
```

Для записи в другой репозиторий используется `EXTERNAL_REPO_PAT`.

### Важные файлы, которые сохраняются при деплое

- `.git/`
- `.github/`
- `.gitignore`
- `README.md`
- `LICENSE`
- `CNAME`

Остальное содержимое репозитория **полностью заменяется** контентом артефакта.

---

## 📚 Дополнительные ресурсы

- [GitHub Actions Artifacts Documentation](https://docs.github.com/en/actions/using-workflows/storing-workflow-data-as-artifacts)
- [actions/download-artifact v4](https://github.com/actions/download-artifact)
- [actions/upload-artifact v4](https://github.com/actions/upload-artifact)
- [Creating a Personal Access Token](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token)

---

## 💡 Best Practices

1. **Именование артефактов:** Используйте осмысленные имена в `output_dir`
2. **Глубина crawl:** Начните с `depth_level: 2`, увеличивайте только при необходимости
3. **Проверка артефактов:** Всегда проверяйте Summary перед деплоем
4. **Безопасность токенов:** Никогда не commit'ьте PAT в код
5. **Мониторинг:** Следите за размером артефактов (влияет на storage billing)

---

**Вопросы?** Создайте issue в репозитории!
